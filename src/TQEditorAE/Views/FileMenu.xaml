<UserControl x:Class="TQEditorAE.Views.FileMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:local="clr-namespace:TQEditorAE.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" ></ColumnDefinition>
            <ColumnDefinition Width="auto" ></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" Grid.Column="0" Margin="2">
            <MenuItem Header="{Binding FileMenuName}">
                <MenuItem Header="{Binding OptionsMenuName}" Command="{Binding OpenOptionsDialogCommand}" />
                <Separator />
                <MenuItem Header="{Binding ExitMenuName}" />
            </MenuItem>
        </Menu>
        <Button Grid.Row="0" Grid.Column="1" Margin="2" Command="{Binding CommitChangesCommand}">
            <TextBlock Text="Commit Changes" Margin="2" />
        </Button>
    </Grid>
</UserControl>
